# OpenAlex polite pool — set to your email to get faster API responses
# (200-500ms vs 1-2s under load). Optional; the API works without it.
# Copy this file to .env.local and fill in your address.
OPENALEX_MAILTO=
